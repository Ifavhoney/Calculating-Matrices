#ifndef a4_h
#define a4_h

#include <stdio.h>
//lastname
int hash1(char *s, int max);
int hash2(char *string, int hash_size);
int hash3(char *string, int hash_size);
int reduce(char c, int max);
//void create_hash( struct array *arrptr, int hash_size )

#endif
